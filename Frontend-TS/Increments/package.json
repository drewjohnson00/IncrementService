{
  "name": "increments-client",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "clean": "rimraf wwwroot/js"
  },
  "devDependencies": {
    "eslint": "^9.39.1",
    "rimraf": "^5.0.0",
    "typescript": "^5.4.2"
  }
}
